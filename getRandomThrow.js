export function getRandomThrow(compThrow){
    // const compChoice = Math.round(Math.random() * 3);
  
    if (compThrow === 1) {
        return 'rock';
    }
    if (compThrow === 2) {
        return 'paper';
    }
    if (compThrow === 3) {
        return 'scissors';
    }
    
}

